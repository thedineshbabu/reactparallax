import React from "react";
import { ParallaxLayer } from "@react-spring/parallax";

const Contact = () => {
  return (
    <ParallaxLayer offset={3} speed={1} style={{ backgroundColor: "#89756" }} />
  );
};

export default Contact;
